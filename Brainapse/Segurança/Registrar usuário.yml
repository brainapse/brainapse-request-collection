info:
  name: Registrar usuário
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/auth/register_user"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "firstName": "John",
          "lastName": "Doe",
          "email": "anderson.santos.gusmao@gmail.com",
          "password": "test123",
          "otpCode": "903081"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
