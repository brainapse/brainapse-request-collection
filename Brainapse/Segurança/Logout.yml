info:
  name: Logout
  type: http
  seq: 6

http:
  method: POST
  url: http://{{url}}/auth/logout
  headers:
    - name: Refresh-Token
      value: "{{refresh_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
