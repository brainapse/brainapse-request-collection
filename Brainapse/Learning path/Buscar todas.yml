info:
  name: Buscar todas
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/learningpath/getall"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
